import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Model Range — 30 m² and 40 m² Modular Homes",
  description:
    "Explore Klara Nordic Modules: fully finished 30 m² and 40 m² modular homes, built in Finland and delivered ready for installation.",
};

export default function ModelRangeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}